export const sqrt = Math.sqrt;
    export function squareroot(x) {
        return x * x;
    }
    export function parameters(x, y) {
        return sqrt(square(x) + square(y));
    }
console.log(squareroot(19)); 
    console.log(parameters(8,9));
